1) OffersPage.tsx - dynamic elements based on two things - the path and the current auth user. 
1.1.a) /offers - loads the title "manage my offers" and components OfferWithPostingComponen
1.1.b) /offers/:postingId (only accessible if current user=owner of the posting) - the title is "manage offers for /posting.title/' and components are OfferComponent.
1.2.a) current user=client loads all offers made to their own postings (ie fetch all offers where offer.posting.client.id==currentuser.findclientIdbyuserId AND activitystatus from base entity = ACTIVE )
1.2.b) current user=freelancer loads all offers they made to any postings (ie all offers where offer.freelancerId==currentUser.findfreelancerbyuserId AND activitystatus from base entity = ACTIVE)
1.3) filtering should work thru OffersFilters component based on an offer's status from the enum OfferStatus AND the activitystatus from base entity=ACTIVE

2) OfferWithPostingComponent - loads on OffersPage if its accessed by /offers, must have this info in the type based on whos accessing:
2.1) if currentuser=freelancer:
- from posting: status, categoryName, (fetch from category), photoUrl, deadline, budget, title
- from client: userId
- from user by client id: photoUrl, rating, reviews_count, firstName, lastName
- from offer: message, status (from OfferStatus enum), proposed price, proposed deadline
2.2) if currentuser=client:
- from posting: status, categoryName (fetch from category), photoUrl, deadline, budget, title
- from freelancer: userId, skills (fetch from freelancer_skills and skills), experience level (from enum)
- from user by freelancer id: photoUrl, rating, reviews_count, firstName, lastName
- from offer: message, status (from OfferStatus enum), proposed price, proposed deadline

3) OfferComponent - loads on OffersPage if its accessed by /offers/:postingId by the CLIENT OWNER OF THE POSTING ONLY, must have this info in the type:
- from freelancer: userId, skills (fetch from freelancer_skills and skills), experience level (from enum)
- from user by freelancer id: photoUrl, rating, reviews_count, firstName, lastName
- from offer: message, status (from OfferStatus enum), proposed price, proposed deadline

-------------------------------------------------------------------------------------------------------------------------------

1. CreateOffer.tsx page - has 5 access requirements: 1) currentuser.role==freelancer 2) currentuser.findfreelancerbyuserid MUSTNT HAVE made an offer to the posting (needs a query to find in offers if there's a match in the postingid+freelancerid combo where status!=pending/shortlisted/rejected and if true - cant access this page) 3) currentuser.freelancer.availability!=NOT_AVAILABLE 4) currentuser.freelancer.completeness<100, and 5) posting.status!=closed. the same requirements must be applied to the 'Apply' button in PostingActions.tsx where currently are applied only 1 and 5. Also an offer wont be able to be edited. the attachments need to work with uploadcare the same way as in CreatePostingForm.tsx

1.1) dto/type fields needed:
- from freelancer: id, userId, cv (from uploadedCV table), availability (from enum), completeness (from these only id gets inserted into offer)
- from user by freelancer id: firstName, lastName, rating, reviews_count, email, location, photoUrl (from these none get inserted into offer)
- from posting: id, title, category, status, photoUrl, deadline, budget (from these only id gets inserted into offer)
- to offer: price, message, delivery time, status (all get inserted into offer with status=pending always)
- to offer_attachments: offer_id, attachment_url

1.2) validation and error handling needed:
- all fields are mandatory besides attachments - message, delivery time, price, deactivate submit button if some are empty/wrong
- all fields should be validated immediately after they lose typing focus
- the delivery time (date) cannot be before current date
- if the delivery time of the offer exceeds the date of the posting deadline, show a warning next to the field, but allow it
- if the budget of the offer exceeds the budget of the posting, display a warning next to the field, but allow it

---------------------------------------------------------------------------------------------------------------------------------

1. ViewOfferPage.tsx - page has 2 access requirements: 1) if currentuser.role==client -> access ONLY if its the owner of the offer's posting (if clientid==offer.posting.clientid); 2) if currentuser.role==freelancer -> access ONLY if its the owner of the offer (if freelancerid==offer.freelancer.id). accessed by offers/:offerId

1.1) dto/type fields needed:
-from freelancer: userId, title, experienceLevel (from enum), availability (from Enum), website, cv (from UploadedCv table), 
-from user: rating, reviews_count, email, location, photoUrl, isVerified, firstName, lastName, phone
-from offer: id, status, price, delivery time, attachments (from offer_attachments table)
-from posting: status, title, category, photoUrl, description, deadline, budget, id

1.2) action buttons dynamic loading and access control in 2 cases each for the 2 roles client and freelancer. THE MESSAGE BTN IS ALWAYS PRESENT:
1.2.1) based on offer status for FREELANCER:
- offer.status==pending/shortlisted -> button cancel
- offer.status==accepted/rejected -> only msg
- offer.status==canceled AND if theres NOT another offer to this posting by this freelancer with status (pending, accepted, shortlisted) AND if the posting status is active -> button reapply (changes status to pending) 
1.2.2) based on offer status for CLIENT:
- offer.status==pending -> buttons shortlist, accept, reject
- offer.status==shortlist -> buttons unshortlist, accept, reject
- offer.status==accepted/rejected/canceled -> only msg
1.2.3) based on freelancer.availability for FREELANCER:
- availability==available/busy -> cancel
- availability==not available -> only msg
1.2.4) based on freelancer.availability for CLIENT:
-availability==available/busy -> accept, reject, shortlist
-availability==not available -> only shortlist

1.3) automated actions:
- if the posting status changes to closed, all the offers should automatically become canceled
- if the freelancer's availability becomes not available - all their offers should become canceled
- not sure how to handle the posting's deadline passing - i dont wanna add new statuses expired/passed or prompt the client to extend the deadline, figure out a way to make it work with the current setup

Final Plan Summary

Frontend Changes
Types - Create/define OfferDetailDto with all fields from 1.1
API - Add offerService.getOfferById(offerId) method
Hook - Update useOfferActionsAccessControl:
Access control: client checks posting ownership, freelancer checks offer ownership
Buttons based on offer status + freelancer availability + posting status
Add canReapply (freelancer + canceled + posting active + no other active offer)
Component - Update OfferActions:
Add "Reapply" button
Add "Unshortlist" button
Show notification badge when reopening posting has canceled offers

Backend Changes
DTO - Create OfferDetailDto with all related data (user, freelancer, posting, cv)
Endpoint - GET /offers/{offerId} - returns full offer details
Endpoint - PUT /offers/{offerId}/status - update status (shortlist/unshortlist/accept/reject/cancel/reactivate)
Automated Actions:
When posting.status → CLOSED → all offers → CANCELED
When freelancer.availability → NOT_AVAILABLE → all their active offers → CANCELED
When posting.deadline passes → all offers → CANCELED
Reopen Notification - When posting reopened, return count of canceled offers for UI notification

Final Button Conditions
Shortlist/Unshortlist    ->	posting.status = active AND offer.status ∈ {pending, shortlisted} (toggle based on current status)
Cancel (Freelancer)   ->	offer.status ∈ {pending, shortlisted} AND posting.status = active AND availability ∈ {available, busy}
Reapply (Freelancer)	->    offer.status = canceled AND posting.status = active AND no other active offer
Accept (Client)    ->    	offer.status ∈ {pending, shortlisted} AND posting.status = active AND freelancer.availability ∈ {available, busy}
Reject (Client)    ->	offer.status ∈ {pending, shortlisted} AND posting.status = active AND freelancer.availability ∈ {available, busy}
Shortlist Only (Client)   ->   freelancer.availability = not_available AND posting.status = active






